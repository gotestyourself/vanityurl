# gotest.tools - vanity url pages

Static pages used to provide the gotest.tools vanity import path.

Run `./render.sh` to rebuild the pages. Pages need to be rebuilt after any new
package are added.
